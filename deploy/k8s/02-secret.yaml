# ---------------------------------------------------------------------------
# 敏感信息（Secret）：DB 凭据等。生产环境建议改用外部密钥管理系统
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: orbit-scheduler-secret
  namespace: orbit-system
  labels:
    app: orbit-scheduler
type: Opaque
stringData:
  DB_USER: orbit
  DB_PASSWORD: orbit123      # TODO: 替换为真实凭据
  # DISPATCH_SECRET: change-me   # 与 orbit.scheduler.http-dispatch.secret 配合使用
