<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.orbit</groupId>
    <artifactId>orbit-batch-scheduler</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>Orbit Batch Scheduler</name>
    <description>轻量云原生批量调度框架（调度中心 + 执行器），类似 XXL-JOB</description>

    <!--
      本 POM 只是「聚合器（aggregator）」：仅负责一次性构建下列模块，
      不作为任何模块的 <parent>，也不下发 properties / dependencyManagement / 插件配置。

      设计取舍（各模块 POM 自治）：
      · 每个模块独立声明自己的坐标、编译级别、依赖与插件版本，可脱离本仓库单独构建；
      · 依赖版本各自通过 import spring-boot-dependencies BOM 收敛，不再依靠父 POM 属性覆盖
        （BOM 用 import 引入时，父级 <properties> 覆盖版本的写法是无效的，必须显式声明版本 —— 
         例如 orbit-admin 的 Quartz 2.5.2 就在其自身 dependencyManagement 中显式锁定）；
      · 模块间引用统一用 ${project.version}，同版本号发布，避免版本漂移。

      构建方式（聚合与继承相互独立，reactor 依旧可用）：
        mvn clean package -DskipTests                 # 全量
        mvn clean package -DskipTests -pl orbit-admin -am   # 单模块 + 其依赖
        cd orbit-core && mvn install                  # 也可完全独立构建
    -->
    <modules>
        <module>orbit-core</module>
        <module>orbit-admin</module>
        <module>orbit-executor</module>
        <module>orbit-executor-sample</module>
    </modules>

</project>
